#include<stdio.h>
int main(){
    char c;
    char str[50];

    printf("enter a character :");
    gets(c);

    printf("enter a string :");
    gets(str);

    printf("enterd characeter is %c", c);

    
return 0;
}